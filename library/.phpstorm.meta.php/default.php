<?php

namespace PHPSTORM_META
{
    
    override(\Model(0), map([
        '' => '@Model',
    ]));
    
    override(\model(0), map([
        '' => '@Model',
    ]));
    
    override(\Logic(0), map([
        '' => '@',
    ]));
    
    override(\logic(0), map([
        '' => '@',
    ]));
}
